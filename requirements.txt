# Core framework
Flask==3.0.3

# WSGI server for deployment
gunicorn==23.0.0

# Database ORM
SQLAlchemy==2.0.34
Flask-SQLAlchemy==3.1.1

# For date/time handling in models
Flask-Migrate==4.0.7

# Utilities (JSON handling, CORS if needed)
Flask-Cors==4.0.1

# Optional: if you use dotenv for environment variables
python-dotenv==1.0.1

# Optional (only if using charts, safe filter, etc.)
itsdangerous==2.2.0
Jinja2==3.1.4
Werkzeug==3.0.4
